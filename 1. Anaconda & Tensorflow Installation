# Environment setup

This guide is superseded by the root README and environment.yml.

Create the CPU-first environment with:

    conda env create -f environment.yml
    conda activate tf2-object-detection

TensorFlow is installed from official pip wheels inside Conda. GPU-specific TensorFlow, CUDA, and global environment-variable steps are intentionally not required.
