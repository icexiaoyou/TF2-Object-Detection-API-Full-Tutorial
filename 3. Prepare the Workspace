# Workspace preparation

Use workspace/training_demo for project data, records, configurations, checkpoints, and exported models.

The tracked models/ directory is an archived historical snapshot. The active Model Garden is installed separately at third_party/models by scripts/setup_object_detection.ps1, so it can be updated without changing system-wide settings.
